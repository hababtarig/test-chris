{{-- resources/views/servers/index.blade.php --}}
<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl">Server user management</h2>
    </x-slot>

    <div class="p-6">
        @if (session('status'))
            <x-alert>{{ session('status') }}</x-alert>
        @endif

        <form action="{{ route('servers.users.store') }}" method="POST" class="space-y-4">
            @csrf
            <x-input-label value="Username"/>
            <x-text-input name="username" />
            <x-input-error for="username"/>

            <x-input-label value="Password"/>
            <x-text-input type="password" name="password" />
            <x-input-label value="Confirm password" class="mt-2"/>
            <x-text-input type="password" name="password_confirmation" />
            <x-input-error for="password"/>

            <x-input-label value="SSH public key" class="mt-4"/>
            <textarea name="public_key" rows="4" class="w-full rounded"></textarea>
            <x-input-error for="public_key"/>

            <x-primary-button class="mt-4">Create on all servers</x-primary-button>
        </form>
    </div>
</x-app-layout>
